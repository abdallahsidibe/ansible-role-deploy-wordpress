#SPDX-License-Identifier: MIT-0
---
# vars file for ansible-role-deploy-wordpress

apache_package: "{{ 'apache2' if ansible_facts['os_family'] == 'Debian' else 'httpd' }}"
php_packages:
  - php
  - "{{ 'php-mysql' if ansible_facts['os_family'] == 'Debian' else 'php-mysqlnd' }}"
packages_common:
  - wget
  - unzip
